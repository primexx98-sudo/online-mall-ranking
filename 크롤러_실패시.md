# 올리브영 크롤러 실패 시 — Claude 처리 매뉴얼

**프로젝트 경로**: `C:\Users\thefuture_brand\Desktop\업무\온라인몰 랭킹`

## 실패 감지 (2026-08-12 추가: 카카오톡 자동 알림)

카카오·다이소·올리브영 셋 중 하나라도 실패하면(`main.py`의 `try/except`가
0건도 실패로 간주) `crawlers/notify.py`의 `notify_kakao_failure()`가
**카카오톡 "나에게 보내기"로 실패한 플랫폼명 + Actions 링크를 즉시 전송**한다.
GitHub 기본 실패 이메일보다 인지가 빠르므로, 이제는 이메일을 놓쳐도 카톡으로
확인 가능하다.

- 알림 조건: GitHub Secrets에 `KAKAO_REST_API_KEY`(REST API 키), `KAKAO_REFRESH_TOKEN`,
  `KAKAO_CLIENT_SECRET`(이 앱은 "클라이언트 시크릿 사용"이 켜져 있어 필수)이
  모두 설정되어 있어야 함. `KAKAO_REST_API_KEY`·`KAKAO_REFRESH_TOKEN` 중 하나라도
  없으면 알림만 조용히 생략되고 크롤링/저장/커밋은 평소대로 진행됨(Actions 로그에
  `[알림 생략] KAKAO_REST_API_KEY/KAKAO_REFRESH_TOKEN 미설정` 출력).
- **refresh token은 앱 키 화면이 아니라 실제 카카오 로그인 동의를 거쳐야
  발급된다.** 재발급이 필요할 때(최초 설정, 또는 refresh_token 만료 시):
  1. 브라우저에서 다음 URL 접속 후 로그인·동의:
     `https://kauth.kakao.com/oauth/authorize?client_id=<REST_API_KEY>&redirect_uri=<등록된 리다이렉트 URI>&response_type=code&scope=talk_message`
     (리다이렉트 URI는 카카오 디벨로퍼스 앱 키 화면의 "카카오 로그인 리다이렉트 URI"에 등록된 값)
  2. 리다이렉트된 `.../?code=...`의 code 값을 복사(유효시간 짧음, 1회용)
  3. PowerShell에서 토큰 교환 + 시크릿 등록을 한 번에:
     ```powershell
     $body = @{
         grant_type    = "authorization_code"
         client_id     = "<REST_API_KEY>"
         client_secret = "<CLIENT_SECRET>"
         redirect_uri  = "<등록된 리다이렉트 URI>"
         code          = "<위에서 받은 code>"
     }
     $response = Invoke-RestMethod -Uri "https://kauth.kakao.com/oauth/token" -Method Post -Body $body
     $response.refresh_token | gh secret set KAKAO_REFRESH_TOKEN --repo primexx98-sudo/online-mall-ranking
     ```
  - **주의**: `client_secret`/`code` 자리에 꺾쇠괄호(`< >`)까지 남기지 말고
    실제 값으로 통째로 교체할 것 (2026-08-12 최초 설정 때 이 실수로 몇 차례
    실패했음).
- 알림이 갑자기 끊기면 Actions 로그에서 `[알림 실패] 카카오톡 전송 중 오류: ...`를
  확인 — `invalid_client`면 REST_API_KEY/CLIENT_SECRET 재확인(둘 다 카카오
  디벨로퍼스에서 복사 버튼으로 직접 복사할 것, 화면을 읽어 옮겨적으면 l/I/1,
  O/0 오독 위험), `invalid_grant`면 위 refresh token 재발급 절차 수행.
- 카톡 알림이 왔든 안 왔든, 아래 수동 보완 절차 자체는 동일하게 필요하다
  (알림은 "실패를 더 빨리 아는" 용도일 뿐, 데이터 복구는 여전히 수동).

## 수동 보완 절차

Actions에서 올리브영 수집이 실패했을 때(카카오톡 알림, 이메일 알림, 또는
Actions 탭에서 확인), 이 파일 + 랭킹 페이지 캡쳐 이미지를 Claude Code에
함께 전달하면 아래 절차를 그대로 수행하면 됩니다.

## 배경 (왜 이 절차가 필요한가)

이전 프로젝트(Health-Ranking, `Desktop\업무\시장조사`)에서 올리브영은 GitHub
Actions(Azure 데이터센터 IP)에서 Cloudflare Turnstile 인터랙티브 챌린지로
막혔던 이력이 있다. stealth 제거, 체크박스 자동 클릭, 헤더 조합, IP 랜덤
재시도까지 모두 시도했으나 코드 레벨로는 우회 불가로 결론남(이전 프로젝트
설계서.md 6장 참고). 이번 리포에서는 자동 수집을 다시 시도하되, 같은 패턴으로
막힐 경우를 대비해 이 수동 보완 절차를 처음부터 마련해둔다.

## Claude가 할 일 (순서대로)

1. **날짜 확인**: 캡쳐가 오늘 날짜 기준인지 사용자에게 확인(다른 언급 없으면 오늘).
2. **`data/daily/YYYY-MM/YYYY-MM-DD.xlsx` 존재 확인**. 없으면 그날 카카오·다이소
   자동 수집 자체가 안 됐거나 그것도 실패한 것 — `git pull`로 최신 상태부터 받고,
   그래도 없으면 `python main.py`로 먼저 그날 수집을 실행해야 함.
3. **캡쳐 이미지를 Read 도구로 읽어 TOP10 추출**: 순위, 브랜드, 상품명, 가격(할인가
   있으면 할인가, 없으면 정가). 상품URL은 캡쳐에 안 보이므로 생략(빈 값).
   - 10개 미만만 보이면(스크롤 잘림 등) 사용자에게 나머지 캡쳐를 추가 요청.
4. **items.json 작성**: 스크래치패드에 아래 형식으로 저장.
   ```json
   [
     {"순위": 1, "브랜드": "...", "상품명": "...", "가격": "8500"},
     ...
   ]
   ```
5. **주입 스크립트 실행** (프로젝트 루트에서):
   ```bash
   python manual_oliveyoung.py <YYYY-MM-DD> <items.json 경로>
   ```
   → `data/daily/YYYY-MM/YYYY-MM-DD.xlsx`의 "올리브영" 시트가 교체되고
   "카테고리통계" 시트가 재계산됨.
6. **결과 검증**: 해당 xlsx를 pandas로 열어 "올리브영" 시트 행 수(=캡쳐로 준 개수)와
   "카테고리통계" 합계가 맞는지 확인.
7. **git 반영**: `git pull --rebase` → `git add data/daily/...` →
   `git commit -m "..."` → `git push`.
8. **사용자에게 결과 요약 보고**: 반영된 개수, 카테고리 분류 결과, push 완료 여부.

## 참고

- 스크립트: `manual_oliveyoung.py` (프로젝트 루트)
- 카테고리 분류는 `crawlers/classifier.py`의 `classify()`가 상품명·브랜드 키워드로 자동 판정
