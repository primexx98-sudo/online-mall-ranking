# 온라인몰 건강기능식품 판매순위 크롤러 설계서

최초 작성: 2026-07-22

> 히스토리·의사결정 과정·시행착오는 **기록.md** 참고. 이 문서는 현재 동작 중인
> 로직/구조만 담는다.

---

## 1. 프로젝트 개요

올리브영 · 다이소몰 · 카카오 선물하기 건강기능식품 판매순위 TOP10을 매일 자동
수집하여 일별 엑셀로 저장하고, 월별 평균순위 TOP20을 집계한다. 상품을
카테고리로 자동 분류하여 일별·월별 카테고리 비율 통계 시트를 함께 생성한다.

**이전 프로젝트에서 이어짐**: `Desktop\업무\시장조사`(Health-Ranking 리포)에서
동일한 목적으로 운영하던 프로젝트가 올리브영 크롤링이 GitHub Actions에서
반복적으로 Cloudflare에 막히면서, 리포를 완전히 새로 만들어 코드를 전면
재작성하기로 하고 시작한 새 프로젝트다. 이전 리포는 그대로 두고 병행하지 않는다.

---

## 2. GitHub 저장소

| 항목 | 값 |
|------|----|
| 리포 | https://github.com/primexx98-sudo/online-mall-ranking (Public) |
| 계정 | primexx98-sudo |
| 브랜치 | master |
| 로컬 경로 | `C:\Users\thefuture_brand\Desktop\업무\온라인몰 랭킹` |

---

## 3. 수집 대상

| 플랫폼 | URL | 카테고리 필터 | 수집 범위 |
|--------|-----|----------|-----------|
| 카카오 선물하기 | `gift.kakao.com/category/5/subcategory/99` (건강식품·영양제) | - | 추천순 TOP10 (광고 제외) |
| 카카오 선물하기 | `gift.kakao.com/category/5/subcategory/100` (다이어트·이너뷰티) | - | 추천순 TOP10 (광고 제외) |
| 다이소몰 | `daisomall.co.kr/ds/exhCtgr/C208/CTGR_00022/CTGR_01020` | - | 실시간 랭킹(`.nav-rank`) TOP10 |
| 올리브영 | `oliveyoung.co.kr/store/main/getBestList.do` | `dispCatNo=900000100100001&fltDispCatNo=10000020001` | 판매랭킹 건강식품 TOP10 |

수집 컬럼: `카테고리 | 순위 | 상품명 | 브랜드 | 가격 | 상품URL | 이미지URL`

> **이미지URL 컬럼 (2026-07-22 추가)**: [[project_health_trend]] 대시보드의 이커머스 카드에 상품 사진을 표시하기 위해 추가. 카카오는 상품 상세페이지 og:image를 `requests`로 별도 조회, 다이소는 브랜드 조회용으로 이미 방문하던 상세페이지에서 og:image 재활용(추가 요청 없음), 올리브영은 이미 Selenium으로 렌더링된 목록 페이지 HTML에서 `<img src>`를 바로 추출(추가 요청 없음, 목록 페이지에 지연 로딩 없이 실제 URL이 박혀있음을 확인). `manual_oliveyoung.py`로 캡쳐 수동 입력된 날은 이미지URL이 공란(스크린샷에서 이미지 URL을 알 수 없음).
>
> **엑셀 좌측 이미지 임베드 (2026-08-24 추가)**: 이 컬럼 값을 재사용해 각 플랫폼
> 시트 A열에 실제 썸네일 이미지를 삽입한다. 8절 참고.

> **올리브영 URL 변경 이력(2026-07-22 확인)**: 이전 프로젝트가 쓰던
> `dispCatNo=900000100100001` 단독 URL은 현재 "전체 판매랭킹"(코스메틱 포함
> 전체)을 반환하도록 사이트 구조가 바뀌어 있었다. 현재는 베스트 랭킹 페이지의
> 건강식품 필터 버튼을 클릭하면 `fltDispCatNo=10000020001`(사이트 전체
> 카테고리 트리의 "건강식품" 코드)이 추가로 붙는 것을 확인해 반영함. 사이트가
> 또 바뀌면 `crawlers/config.py`의 URL을 재확인해야 한다.

---

## 4. 파일 구조

```
C:\Users\thefuture_brand\Desktop\업무\온라인몰 랭킹\   ← 로컬 & GitHub 리포 루트
│
├── .github/workflows/
│   ├── daily_crawl.yml          # 매일 09:03 KST 자동 실행 (2026-07-23 정각 지연 회피로 변경)
│   └── monthly_aggregate.yml    # 매월 1일 09:00 KST 자동 실행
│
├── crawlers/
│   ├── config.py                 # ★ URL 변경 시 이 파일만 수정
│   ├── classifier.py             # ★ 카테고리 추가/수정 시 이 파일만 수정
│   ├── base.py                   # Selenium 공통 유틸 (new_driver())
│   ├── notify.py                 # 실패 시 카카오톡 알림 (2026-08-12 추가)
│   ├── excel_image.py            # 시트 A열에 제품 썸네일 이미지 임베드 (2026-08-24 추가)
│   ├── kakao.py / daiso.py / oliveyoung.py
│
├── data/
│   ├── daily/   YYYY-MM/YYYY-MM-DD.xlsx   # 일별 수집본 (시트 4개)
│   └── monthly/ YYYY-MM_월별취합.xlsx      # 월별취합 (시트 6개: 월별요약 포함)
│
├── main.py                # 일별 수집 진입점
├── monthly_aggregate.py   # 월별 집계
├── manual_oliveyoung.py   # 올리브영 실패 시 캡쳐 기반 수동 입력
├── requirements.txt
├── 설계서.md / README.md / 기록.md / 크롤러_실패시.md
```

---

## 5. 카테고리 분류 (`crawlers/classifier.py`)

이전 프로젝트(Health-Ranking)의 11개 카테고리 키워드 맵을 그대로 이식했다.
상품명+브랜드명을 소문자 변환 후 `CATEGORY_MAP` 리스트 순서대로 키워드 매칭,
첫 매칭 카테고리 1개 배정(미매칭 시 `기타/특수목적`).

**2026-07-29: 키워드 세트 재이식** — 재구축 당시 옮겨온 키워드가 축약판이었음이
드러나 이전 프로젝트의 전체 키워드 맵으로 다시 교체(예: 비타민C 항목에
"비타민 c", "아스코르빈" 등 띄어쓰기·이형 표기 추가).

| 순위 | 카테고리 | 주요 키워드 |
|------|---------|------------|
| 1 | 멀티비타민/종합비타민 | 멀티비타민, 종합비타민, 멀티 비타민, 멀티미네랄, 이뮨비타민, 이뮨 비타민, 종합 비타민 |
| 2 | 비타민C/항산화 | 비타민c, 비타민 c, c1000, c 1000, 항산화, 아스코르브, 아스코르빈 |
| 3 | 비타민D | 비타민d, 비타민 d, 비타민디 |
| 4 | 마그네슘 | 마그네슘 |
| 5 | 루테인/눈건강 | 루테인, 제아잔틴, 아스타잔틴, 눈건강, 눈 건강, 오메가3, 오메가-3, omega3 |
| 6 | 혈행 | 코엔자임, q10, 코큐텐, 혈행, 혈액순환 |
| 7 | 홍삼/인삼 | 홍삼, 인삼, 고려홍삼, 홍경천, 정관장 |
| 8 | 다이어트/체중관리 | 다이어트, 체중관리, 슬리밍, 쾌변, 체지방, 가르시니아, 콜레올로지, 닭가슴살, 단백질, 아르기닌, 크레아틴, 혈당, 바나바 |
| 9 | 프로바이오틱스/유산균 | 프로바이오틱스, 유산균, 락토바실러스, 비피더스, 생유산균 |
| 10 | 피부/미용 | 콜라겐, 피부, 미용, 히알루론산, 비오틴, 이너뷰티, 더마 |
| 11 | 기타/특수목적 | (위 카테고리 미해당 전부) |

추가/수정: `CATEGORY_MAP` 리스트에 항목 추가. 리스트 순서 = 우선순위.

---

## 6. 크롤러 방식

세 플랫폼 모두 Selenium(headless Chromium)을 사용한다(`crawlers/base.py`의
`new_driver()`로 옵션 공유). 카카오·다이소는 페이지 로드 후
`execute_script()`로 Playwright 시절과 동일한 JS 추출 로직을 그대로 재사용,
올리브영은 아래 별도 방식(`time.sleep()`)을 유지한다.

**2026-07-29: 카카오·다이소를 Playwright → Selenium으로 통일** — 기능상
문제(차단 등)는 없었으나 코드 일관성을 위해 통일. Playwright/
`playwright-stealth` 의존성 완전 제거로 `requirements.txt`·GitHub Actions
설치 단계(`playwright install --with-deps chromium`)도 함께 정리됨. 다이소의
상세페이지 방문은 Playwright의 `context.new_page()`(새 탭, 세션 공유) 대신
Selenium의 `driver.switch_to.new_window("tab")`으로 동일하게 구현.

| 플랫폼 | 핵심 셀렉터 | 비고 |
|--------|-----------|------|
| 카카오 | `gc-product` 컨테이너, `span.txt_prdbrand`, `strong.txt_prdname`, `.txt_price`, `a.link_prdunit[href]` (`.area_ad` 제외) | 카테고리당 10개×2=최대 20개 |
| 다이소 | `.nav-rank .swiper-slide` (DOM 순서 = 실시간 순위 순서), `.product-title`, `.price-value`, `a.detail-link[href]` | 브랜드는 목록에 없어 TOP10 상세페이지(`.brand-area .detail-title`) 추가 방문 |
| 올리브영 | `ul.cate_prd_list > li`, `span.tx_brand`, `p.tx_name`, `.tx_cur`(현재가), `a[href]` | `time.sleep()` 기반, 3장 URL 참고 |

**올리브영 차단 대응 — Selenium으로 전환 (2026-07-22 결정, 해결됨)**: 이전
프로젝트에서 GitHub Actions(Azure IP)가 Playwright 기반 수집을 Cloudflare
Turnstile로 막는 문제를 코드 레벨로 해결하지 못해 캡쳐 기반 수동 입력으로
전환했던 이력이 있다(기록.md 참고). 이번 리포도 초기엔 동일하게 Playwright로
재현 시도했으나 같은 패턴으로 재차단됨을 확인. 이후 **Selenium +
`time.sleep()` 방식**(`crawlers/oliveyoung_selenium_test.py`로 실증, 이후
`crawlers/oliveyoung.py`에 이식)을 GitHub Actions에서 4회 연속 테스트한 결과
전부 Turnstile 없이 정상 수집(상품 100건)됨을 확인 — Playwright 자체가 아니라
**Playwright의 자동화 시그니처**가 차단 트리거였던 것으로 추정. 현재는 이
Selenium 버전이 운영 코드이며, `manual_oliveyoung.py` + `크롤러_실패시.md`
절차는 예비책(재차단 시 대응)으로 남겨둔다.

---

## 6-1. 엑셀 좌측 이미지 임베드 (`crawlers/excel_image.py`, 2026-08-24 추가)

일별·월별취합 xlsx의 각 플랫폼 시트 맨 앞(A열)에 이미지URL 컬럼 값을
다운로드해 실제 썸네일 이미지로 임베드한다(재수집 없음, 기존 컬럼 재사용).
`insert_image_column(ws, image_urls)`을 해당 시트를 `df.to_excel()`로 쓴
직후 호출하면 A열 앞에 "이미지" 헤더 컬럼을 새로 끼워넣고 각 행에 이미지를
꽂는다 — **호출 시점에 기존 컬럼이 전부 한 칸씩 밀리므로, 열 너비 계산 등
위치에 의존하는 로직은 반드시 이 호출 다음에 와야 한다.**

- 다운로드한 원본 이미지를 그대로 임베드하지 않고, Pillow로 96×96px 이내
  리샘플링 + JPEG 재압축(quality=80) 후 임베드한다. 원본을 그대로 넣고
  `width`/`height` 속성만 표시 크기(48px)로 줄이면 화면 표시만 작아질 뿐
  실제 저장되는 픽셀 데이터는 원본 그대로라 파일이 수십 MB로 불어난다
  (실측: 이미지 40장 기준 55MB → 리샘플링 후 148KB, 기록.md 2026-08-24 참고).
- 개별 이미지 다운로드/디코딩 실패는 그 칸만 비우고 조용히 넘어간다
  (한 상품 실패로 전체 저장 흐름이 막히지 않도록).
- 카테고리통계/브랜드통계/월별요약 시트는 대상 아님(제품 단위 행이 아님).
- **[[project_health_trend]] 연동 주의**: `ecommerce_collector.py`가 일별
  xlsx를 GitHub raw URL로 직접 읽어 컬럼을 위치(인덱스) 기반으로 파싱한다.
  이 리포의 플랫폼 시트 컬럼 순서를 또 바꾸면 그쪽 인덱스 매핑도 항상 같이
  수정해야 한다(현재는 A열 이미지 삽입 반영해 `r[1]`=카테고리 …
  `r[7]`=이미지URL로 맞춰둔 상태). 월별취합 xlsx는 그 컬렉터가 읽지 않아
  컬럼 순서 변경에 영향받지 않는다.

### 카드형 시트 (2026-08-24 추가)

A열 썸네일(48px)이 너무 작다는 피드백에 따라, 순위를 가로로 펼쳐 열마다
상품 하나가 되는 **카드형 시트를 플랫폼마다 별도로 추가**했다(예:
`다이소몰_카드형`). 이미지를 130px로 훨씬 크게 넣는 게 목적이며, 원본 표
시트는 전혀 건드리지 않으므로 월별집계·health-trend 로직에는 영향 없다.

- `insert_card_sheet(wb, sheet_name, blocks, price_label="판매가")` —
  `blocks`는 `[(블록 라벨, items), ...]` 리스트. 새 시트를 만들어 블록마다
  헤더(순위 1위~N위) + 썸네일 + 카테고리 + 브랜드 + 상품명 + 가격 + 링크
  (하이퍼링크) 7줄을 그린다.
- **카카오선물하기는 블록 2개로 분리**: 서브카테고리(건강식품·영양제 /
  다이어트·이너뷰티) 각 10개가 이어붙어 있어 한 줄로 펼치면 순위가
  1위~10위, 1위~10위로 두 번 나오면서 20칸 가로 스크롤이 부담스럽다는
  피드백(2026-08-24) → `main.py`의 `build_card_blocks()`가
  `crawlers/config.py`의 `categories` 설정을 이용해 서브카테고리별로
  블록을 나누고, `insert_card_sheet()`가 세로로 쌓아 그린다(각 블록
  독립적으로 1위~10위 헤더를 가짐). 다이소몰·올리브영은 단일 블록(순위
  그대로 1위~10위 또는 1위~20위).
- 원본 이미지는 280px 이내로 리샘플링 + JPEG 재압축 후 임베드(표 시트용
  96px보다 크게 — 카드형은 이미지 자체가 주 목적이라 더 선명하게).
- **다운로드 캐시(`cache: dict`)**: 같은 상품이 표 시트(작게)·카드형
  시트(크게) 양쪽에 들어가 원본을 두 번 받게 되는데, `main.py`/
  `monthly_aggregate.py`가 파일 하나당 캐시 dict 하나를 만들어
  `insert_image_column()`/`insert_card_sheet()` 양쪽에 넘겨 원본 다운로드를
  1회로 줄인다(URL→원본바이트, 리샘플링은 각 크기별로 별도 수행).
- **월별취합에서 재사용**: `monthly_aggregate.py`의 `write_sheet()`가
  `with_images=True`일 때 표 시트 이미지 삽입에 이어 카드형도 함께 만든다
  (`_to_card_items()`가 `순위(월평균)→순위`, `평균가격→가격`으로 필드명만
  맞춰 변환). 카카오도 월별취합은 서브카테고리 구분 없이 상품 단위로 이미
  통합돼 있어 블록 분리 불필요(단일 블록, TOP20 = 20칸).
- **2026-08-24 백필**: 이 기능 적용 전에 수집된 `data/daily/2026-08/2026-08-01~23.xlsx`
  23개 파일에 재크롤링 없이(기존 이미지URL 컬럼 재사용) A열 썸네일 +
  카드형 시트를 일회성 스크립트로 소급 반영(기록.md 참고). `2026-07_월별취합.xlsx`도
  재생성해 카드형 반영.
- **원본 표 시트 숨김 처리 (2026-08-24 추가)**: 카드형이 같은 정보를 더 크게
  보여주므로, 사용자가 "카드형이랑 카테고리통계만 남게 해달라"고 요청 →
  `crawlers/excel_image.py`의 `hide_sheet(ws)`로 카카오선물하기/다이소몰/
  올리브영 원본 표 시트를 `sheet_state = "hidden"` 처리(탭에서만 숨김,
  **데이터는 그대로 남아 월별집계·health-trend는 전혀 영향받지 않음** —
  `pd.read_excel()`/`openpyxl`이 숨김 시트도 그대로 읽어옴, 엑셀에서 시트
  탭 우클릭 → 숨기기 취소로 언제든 다시 보이게 가능). 월별취합의
  월별요약·카테고리통계·브랜드통계는 카드형과 겹치는 정보가 아니므로
  숨기지 않고 그대로 둠. 원본 표 시트가 숨겨지면서 액티브 탭이 빈 화면으로
  뜨지 않도록 첫 `_카드형` 시트를 활성 탭으로 지정(`wb.active`). 8월 24개
  일별 파일 + 7월 월별취합에도 소급 반영(재다운로드 없이 `sheet_state`만
  일괄 변경).

---

## 7. 로컬 실행

```bash
# 최초 1회
pip install -r requirements.txt
playwright install chromium

# 일별 수집
python main.py               # 오늘 날짜로 저장
python main.py TEST          # data/daily/TEST/ 테스트용 (실제 날짜 폴더 오염 없음)

# 월별 집계
python monthly_aggregate.py            # 지난달 집계
python monthly_aggregate.py YYYY-MM    # 특정 월 집계

# 올리브영 수동 입력 (자동 실패 시)
python manual_oliveyoung.py <YYYY-MM-DD> <items.json>

# Git push
git add data/ && git commit -m "data: YYYY-MM-DD 수집" && git pull --rebase && git push
```

> 엑셀 파일이 열려있으면 PermissionError 발생 → 파일 닫고 재실행

---

## 8. 자동화 (GitHub Actions)

| 워크플로 | 트리거 | 동작 |
|---------|--------|------|
| `daily_crawl.yml` | 매일 UTC 01:07(=KST 10:07) + 수동 | 3개 플랫폼 수집 → 카테고리 분류 → xlsx 저장 → git push |
| `monthly_aggregate.yml` | 매월 1일 UTC 00:00 + 수동 | 전월 daily xlsx 집계 → 카테고리 통계 → 월별 xlsx 저장 → git push |

- **2026-07-23 크론 시각 변경 (2단계)**:
  1. 1차: 저장소 생성(2026-07-22) 이후 정각(`0 0 * * *`) 스케줄이 한 번도 자동 실행된 적이 없어(수동 실행만 존재) `3 0 * * *`(09:03 KST, health-trend 09:13 수집 직전)로 변경
  2. 2차(최종): 사용자가 "올리브영·다이소몰·카카오 선물하기 랭킹은 매일 오전 10시경 갱신된다"는 점을 지적 — 09:03에 돌리면 사이트가 아직 전날 랭킹을 보여주는 시점이라, **파일명은 당일 날짜인데 내용은 실제로는 전전날 랭킹**이 되는 이중 지연 문제가 있었음. 랭킹 갱신 이후인 `7 1 * * *`(10:07 KST)로 재조정 → 이제 파일명=실제 랭킹 날짜가 항상 일치함. 대신 [[project_health_trend]]의 09:13 KST 수집은 이 크롤링보다 항상 먼저 끝나므로, 대시보드 이커머스 카드는 구조적으로 "전일 기준"으로 고정 표시됨(health-trend 쪽에서 명시적으로 라벨링, 4-7 참고)

- **실행 환경**: `ubuntu-22.04` 고정 (이전 프로젝트에서 `ubuntu-latest`=24.04가
  Playwright `libasound2` 호환 문제를 일으켰던 이력 반영)
- **필수 설정**: 두 워크플로 모두 `permissions: contents: write` + commit 후
  `git pull --rebase` 포함 (매월 1일 두 워크플로 동시 실행 시 push 충돌 방지)
- **실패 시**: `main.py`가 플랫폼 1개라도 실패하면 `sys.exit(1)` → GitHub
  기본 워크플로 실패 이메일 알림(계정 이메일)으로도 확인 가능하지만,
  **2026-08-12부터 `crawlers/notify.py`가 실패 즉시 카카오톡 "나에게 보내기"로
  실패한 플랫폼명 + Actions 링크를 전송**한다(`KAKAO_REST_API_KEY`,
  `KAKAO_REFRESH_TOKEN` GitHub Secrets 필요, 자세한 내용은
  `크롤러_실패시.md` "실패 감지" 절 참고). 알림 자체가 실패해도(토큰 만료 등)
  크롤링/저장/커밋 흐름에는 영향 없음(예외를 삼키고 로그만 남김).

---

## 9. 엑셀 파일 구조

### 일별 `data/daily/YYYY-MM/YYYY-MM-DD.xlsx` — 시트 4개

| 시트명 | 내용 |
|--------|------|
| 카카오선물하기 | 건강식품·영양제(1~10위) + 다이어트·이너뷰티(1~10위) = 최대 20행. 컬럼에 이미지URL 포함 (2026-07-22~), A열에 썸네일 이미지 임베드 (2026-08-24~) |
| 다이소몰 / 올리브영 | TOP10. 컬럼에 이미지URL 포함 (2026-07-22~), A열에 썸네일 이미지 임베드 (2026-08-24~) |
| 카테고리통계 | 3개 플랫폼 합산 카테고리별 상품수 + 비율(%), 플랫폼별 내역 |

### 월별 `data/monthly/YYYY-MM_월별취합.xlsx` — 시트 6개 (2026-07-29 이전 프로젝트 로직 이식)

| 시트명 | 내용 |
|--------|------|
| 월별요약 (맨 앞 시트) | 핵심지표·플랫폼별 1위·성장+고순위 카테고리 추천·급상승/급하락 TOP5·카테고리 증감 TOP5·신규진입·신제품 기획 인사이트 9블록을 한 시트에 세로로 이어붙임 |
| 카카오선물하기 / 다이소몰 / 올리브영 | 월평균순위 TOP20 (전월 대비 순위·가격 변동 포함), A열에 썸네일 이미지 임베드 (2026-08-24~) |
| 카테고리통계 | 월간 TOP20 기준 카테고리 비율 (전월 대비 증감 포함) |
| 브랜드통계 | 플랫폼별 TOP20 내 브랜드 등장 횟수 (전월 대비 증감 포함) |

플랫폼 시트 컬럼: `순위(월평균) | 카테고리 | 상품명 | 브랜드 | 이미지URL | 평균가격 | 전월가격 | 가격변동 | 평균순위 | 전월순위 | 순위변동 | 등장횟수`

카테고리통계 컬럼: `카테고리 | 전체 | 비율(%) | 전월비율(%) | 증감(%p) | 카카오선물하기 | 다이소몰 | 올리브영`

브랜드통계 컬럼: `브랜드 | 등장횟수(TOP20 합산) | 비율(%) | 전월등장횟수 | 증감 | 카카오선물하기 | 다이소몰 | 올리브영`

### 상품 그룹핑 (2026-08-05 개편)

같은 상품이라도 올리브영·카카오 등이 프로모션 문구를 거의 매일 바꿔서
(예: "7입"→"8입(7+1)", "1일 1정"→"1일1정", "독일 명품 비타민" 접두사
추가/삭제) 상품명 완전일치로 그룹핑하면 같은 상품이 TOP20 안에 여러 행으로
쪼개지는 문제가 있었다. `build_group_keys()`가 아래 우선순위로 그룹 키를 만든다:

```
1순위: 상품URL의 상품코드 (올리브영 goodsNo / 다이소몰 pdNo / 카카오 /product/{id})
       → 실제로 다른 SKU가 등록되지 않는 한 문구가 바뀌어도 고정
2순위: 정규화된 상품명 (URL 파싱 실패 시 대체) — 대괄호 프로모션 태그 전체 제거 + 공백 전체 제거
       → 대괄호 밖의 실제 구성/용량 표기(14입, 30ml 등)는 남아있어 다른 SKU끼리는 안 섞임
```

같은 상품이 어떤 날은 URL이 잡히고(1순위) 어떤 날은 안 잡히는(2순위 폴백)
경우를 대비해 union-find로 "URL 키"와 "정규화 이름 키"가 한 번이라도 같은
행에서 만나면 하나의 그룹으로 합친다. 전월 대비 순위·가격 비교도 동일한
정규화 함수로 매칭해 문구 흔들림 때문에 "🆕 신규 진입"으로 오판되는 것을 줄였다.

### 월별 집계 로직 (`monthly_aggregate.py`)

```
최소등장   = min(3, 실제수집일수)
           → 필터 후 대상이 20개 미만이면 최소등장을 2회, 1회로 순차 완화해 재필터링
일별점수   = (11 - 순위) × 0.5              # 1위=5점 … 10위=0.5점
전체평균점수 = 이 플랫폼·이 달의 일별점수 전체 평균     # 베이지안 스무딩의 사전확률(prior)
월간점수   = (SUM(일별점수) + K×전체평균점수) / (등장횟수 + K)   # K=5 (BAYESIAN_K)
월간TOP20  = (등장횟수 ≥ 최소등장, 완화 적용 후) 대상 중 월간점수 기준 상위 20개 (표시는 평균순위)
```

**2026-08-05: 월간점수 공식을 베이지안 평균으로 교체.** 기존
`평균점수×0.7 + 등장률×5×0.3` 방식은 2번만 등장해도 그 2번의 평균순위가
좋으면(예: 평균 2.5위) 15~20번 꾸준히 등장한 상품보다 위로 올라가는 왜곡이
있었다. 베이지안 평균은 등장횟수가 적을수록 점수가 전체 평균 쪽으로
당겨지고(가상의 K=5회를 전체평균으로 채워 넣는 효과), 등장횟수가 많을수록
원래 평균점수에 수렴한다 — IMDB 평점 계산과 같은 원리. 실제 7월 올리브영
데이터로 검증: 2회만 등장했던 상품들이 8~10위권 밖으로 밀려나고, 대신
꾸준히 등장했지만 평균순위가 아주 나쁘지는 않은 상품들이 상위권을 채움.

이 최소등장 필터·전월 대비 비교 로직(그룹핑 방식 제외)은 이전 프로젝트
(기록.md 2026-07-01, 2026-07-13 참고)에서 여러 차례 시행착오를 거쳐
확정된 것을 재사용했다.

**2026-07-29: 월별요약·브랜드통계 시트 이식 완료** — 재구축 당시 보류했던
월별요약(신제품 기획 인사이트 포함) 시트와 브랜드통계 시트를 이전 프로젝트
(Health-Ranking, `시장조사` 폴더)에서 `monthly_aggregate.py`·`classifier.py`
그대로 이식(기록.md 참고). 이 시점부로 두 프로젝트가 이 리포 하나로
통합되어, 이전 프로젝트 폴더·GitHub 리포는 삭제됨. **이번 버전에 여전히
없는 것**: Streamlit 대시보드(사용자가 "엑셀 시트로 충분하다"고 명시,
[[project_health_ranking]] 2026-07-13 결정을 계승). 카카오톡 실패 알림은
2026-08-12에 추가됨(8장 참고).

---

## 10. requirements.txt

```
selenium==4.46.0
pandas==3.0.3
openpyxl==3.1.5
requests==2.31.0
beautifulsoup4==4.12.3
Pillow==12.2.0
```

> playwright/playwright-stealth는 6장의 2026-07-29 통일 작업 때 완전 제거됨(이 문서에
> 반영이 누락돼 있던 걸 2026-08-24 이미지 임베드 작업 중 발견해 정정). Pillow는
> 엑셀 이미지 임베드(6-1절)용으로 2026-08-24 추가.

---

## 11. 유지보수 가이드

| 상황 | 조치 파일 | 구체적 수정 위치 |
|------|-----------|----------------|
| 카테고리 추가/수정 | `crawlers/classifier.py` | `CATEGORY_MAP` 리스트에 항목 추가/키워드 수정 |
| 사이트 URL 변경 | `crawlers/config.py` | 해당 플랫폼 `url` 값 수정 |
| 카카오 상품 0개 | `crawlers/kakao.py` | `gc-product`, `span.txt_prdbrand`, `strong.txt_prdname`, `.txt_price` 셀렉터 확인 |
| 다이소 상품 0개 | `crawlers/daiso.py` | `.nav-rank .swiper-slide`, `.product-title`, `.price-value` 셀렉터 확인 |
| 다이소 브랜드 누락 | `crawlers/daiso.py` | `.brand-area .detail-title` 셀렉터 확인 |
| 올리브영 상품 0개 / 다른 카테고리가 섞임 | `crawlers/config.py`, `crawlers/oliveyoung.py` | `dispCatNo`/`fltDispCatNo` 값과 `ul.cate_prd_list`, `span.tx_brand`, `p.tx_name`, `.tx_cur` 셀렉터 확인 (3장 URL 변경 이력 참고) |
| 올리브영 재차단 (Selenium도 다시 막히는 경우) | `크롤러_실패시.md` | 캡쳐 기반 수동 입력 절차로 전환 |
| 실패했는데 카카오톡 알림이 안 옴 | `crawlers/notify.py`, GitHub Secrets | `KAKAO_REST_API_KEY`/`KAKAO_REFRESH_TOKEN` 설정 여부, Actions 로그의 `[알림 실패]`/`[알림 생략]` 메시지, refresh_token 만료(재인증 필요) 확인 |
| Actions push 실패 | GitHub | PAT/기본 `GITHUB_TOKEN` 권한 확인 |
| Actions push 충돌 | `.github/workflows/` | 두 yml 모두 commit 후 `git pull --rebase` 있는지 확인 |
| Actions 권한 오류 | `.github/workflows/` | `permissions: contents: write` 양쪽 yml에 있는지 확인 |
| libasound2 오류 | `.github/workflows/daily_crawl.yml` | `runs-on: ubuntu-22.04` 유지 |
| 엑셀 저장 PermissionError | - | 해당 xlsx 파일을 Excel에서 닫고 재실행 |
| 이미지가 빈 칸으로 나옴 | `crawlers/excel_image.py` | 원본 이미지URL 접근 가능 여부 확인(다운로드 실패 시 조용히 스킵됨), CDN이 hotlink 차단하면 User-Agent 헤더 확인 |
| 플랫폼 시트 컬럼 순서를 바꿈 | [[project_health_trend]]의 `ecommerce_collector.py` | 그쪽 `_row_to_item()` 인덱스도 반드시 같이 수정 (6-1절 참고) |

---

## 12. 현황 (2026-07-22 기준)

- [x] 크롤러 3종 전면 재작성 + 실제 사이트 대상 셀렉터 검증 완료 (로컬 IP)
- [x] 올리브영 URL이 사이트 개편으로 바뀐 것을 발견, `fltDispCatNo` 파라미터
      추가로 수정 (3장 참고) — 이전 프로젝트 이관만으로는 그대로 동작하지
      않았을 것
- [x] 카테고리 분류기 이식 (11개 카테고리, 이전 프로젝트와 동일 키워드)
- [x] 월별 집계 로직 이식 + 합성 데이터로 점수공식·최소등장 완화·전월대비
      비교 검증 완료 (실제 프로젝트 데이터 아닌 스크래치 테스트)
- [x] `main.py` 로컬 실행으로 3개 플랫폼 전체 정상 수집 확인 (2026-07-22)
- [x] GitHub 리포 생성 및 최초 push (2026-07-22, `online-mall-ranking`, Public)
- [x] GitHub Actions 최초 실행 확인 (2026-07-22, workflow_dispatch) — 카카오·
      다이소 정상, **올리브영은 이전 프로젝트와 동일하게 Azure IP에서
      차단됨**(타임아웃, Playwright 기반)
- [x] 올리브영을 Selenium + sleep 방식으로 전면 교체, GitHub Actions에서 4회
      연속 정상 수집(Turnstile 없음) 확인 후 운영 코드로 반영 (2026-07-22,
      기록.md 참고). `크롤러_실패시.md`는 다시 예비책으로 전환
- [x] `daily_crawl.yml` 수동 실행으로 Selenium 버전 종단 검증 완료 (2026-07-22,
      카카오 20건·다이소 10건·올리브영 10건 모두 정상, 기록.md 참고)
- [ ] 최소 1개월 실데이터 누적 후 월별 집계 실행 확인
- [x] 이미지URL 컬럼 추가, 3개 플랫폼 모두 실데이터로 로컬 검증(`python main.py TEST`) 완료, `daily_crawl.yml` 수동 재실행으로 실제 배포본에도 반영 (2026-07-22)
- [x] **크론 미실행 발견·수정 (2026-07-23)**: health-trend 대시보드의 이커머스 카드가 "데이터 기준일 2026-07-22"로 하루 지연 표시되는 걸 발견 — `data/daily/2026-07/`에 7/23자 xlsx가 없었음. 저장소 생성(7/22) 이후 정각(`0 0 * * *`) schedule 트리거가 단 한 번도 실행된 적 없었던 것이 원인(다른 프로젝트들에서 관측된 정각 큐잉 지연과 동일 계열 문제). 수동 크롤링으로 7/23자 데이터 즉시 확보 후, 크론을 처음엔 `3 0 * * *`(09:03 KST)로 옮겼다가, "각 몰의 실제 랭킹 갱신이 오전 10시경"이라는 점을 반영해 최종적으로 `7 1 * * *`(10:07 KST)로 재조정(8절 참고) — health-trend는 계속 09:13 KST에 도는 걸 유지하기로 해서, 이커머스 카드는 구조적으로 "전일 기준"이 되며 대시보드에 그렇게 명시함
- [x] **두 프로젝트 통합 (2026-07-29)**: 이전 프로젝트(Health-Ranking, `시장조사`
      폴더)의 daily 데이터 중 올리브영 시트가 있는 날짜만 이관(2026-06 전체
      30일 + 2026-07 15일, 올리브영 없는 날·이 리포와 겹치는 07-22~28은
      이 리포 자체 데이터를 우선해 미이관), `monthly_aggregate.py`·
      `classifier.py`를 이전 프로젝트의 발전된 버전으로 교체 후 2026-06·
      2026-07 월별취합 재생성. 이전 프로젝트 로컬 폴더와 GitHub 리포
      (Health-Ranking)는 삭제, 이 리포로 완전히 통합됨(기록.md 참고).

> 과거 프로젝트(Health-Ranking)의 상세 트러블슈팅 이력은 위 통합 작업 시
> 기록.md에 요약해 남겨두었다(원본 리포는 2026-07-29 삭제됨).

- [x] **카카오톡 실패 알림 추가 (2026-08-12)**: 8/11 올리브영 크롤링이
      Cloudflare에 재차단되어 0건 수집 → 실패 사실을 GitHub 기본 이메일
      알림에 의존하다 늦게 인지한 것을 계기로, `crawlers/notify.py`를 추가해
      3개 플랫폼 중 하나라도 실패하면 카카오톡 "나에게 보내기"로 즉시
      알림이 가도록 함(8장 참고). GitHub Secrets 3종(`KAKAO_REST_API_KEY`,
      `KAKAO_REFRESH_TOKEN`, `KAKAO_CLIENT_SECRET` — 이 앱은 클라이언트
      시크릿 사용이 켜져 있어 필수)을 사용자가 직접 등록. 실제 시크릿 값은
      Claude 세션에 노출하지 않는 방식으로 진행했으나, refresh_token 발급
      과정에서 앱 키(REST API 키)와 클라이언트 시크릿 값을 사용자가 두 차례
      직접 채팅에 붙여넣게 됨 — REST API 키는 노출돼도 위험도가 낮지만
      클라이언트 시크릿은 민감값이라 원칙적으로는 피했어야 함(크롤러_실패시.md
      참고, 재발급 시엔 이 문서의 절차대로 사용자 터미널에서만 다루기).
      전용 임시 워크플로(`test_notify.yml`)로 실제 GitHub Actions 환경에서
      발송까지 검증 후 삭제. 카카오 디벨로퍼스 "앱 키" 화면은 REST API
      키·네이티브 앱 키 등을 마스킹 없이 그대로 보여주는 UI라 캡쳐 공유
      시 유의(클라이언트 시크릿 코드도 별도 화면에서 마스킹 없이 노출됨).
