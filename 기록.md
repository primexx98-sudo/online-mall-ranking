# 기록 (히스토리 · 의사결정 로그)

> 설계서.md는 현재 동작 중인 로직/구조만 담는다. "왜 이렇게 바뀌었는지",
> "과거에 뭘 했는지"는 여기에 기록한다.

---

## 프로젝트 시작 — 리포 재구축 결정 (2026-07-22)

**배경**: 이전 프로젝트(Health-Ranking, `Desktop\업무\시장조사`)에서 올리브영
크롤러가 GitHub Actions(Azure 데이터센터 IP)에서 Cloudflare Turnstile
인터랙티브 챌린지로 반복 차단됨. 코드 레벨(stealth, 자동클릭, 헤더 위장,
IP 랜덤 재시도 9회)로 모두 시도했으나 해결 불가로 결론나 캡쳐 기반 수동
입력으로 우회해 운영 중이었음(이전 리포 기록.md 2026-07-13 항목 참고).

사용자가 "올리브영 크롤링이 자꾸 막힌다"며 GitHub 리포를 처음부터 새로
만드는 방식으로 다시 시도해보고 싶다고 요청. 새로 시작하기 전에 **리포를
새로 만드는 것 자체는 Azure IP 차단 문제를 해결하지 못한다**는 점(리포가
바뀌어도 Actions 실행 IP 대역은 동일)을 먼저 짚고, 방향을 확인하는 인터뷰
진행.

| 항목 | 결정 |
|------|------|
| 올리브영 수집 방식 | 이번엔 자동 수집 재시도부터 (프록시/self-hosted 등 IP 레벨 대응은 보류) |
| 코드 | 전면 재작성 (기존 코드 복사 대신 처음부터 새로 작성) |
| 기존 Health-Ranking 리포 | 그대로 두고 새 폴더만 생성 (삭제/아카이브 안 함) |

**경로**: `C:\Users\thefuture_brand\Desktop\업무\온라인몰 랭킹` (새 로컬 폴더,
GitHub 리포도 새로 생성 예정)

---

## 전면 재작성 중 발견한 사이트 변경사항 (2026-07-22)

크롤러를 처음부터 새로 작성하면서, 셀렉터를 추측이 아니라 실제 사이트를
Playwright로 직접 열어 하나씩 검증했다(로컬 가정용 IP 기준).

**카카오 선물하기**: `gc-product`, `span.txt_prdbrand`, `strong.txt_prdname`,
`.area_ad` 광고 제외 — 이전 프로젝트와 동일하게 여전히 유효함. 가격 셀렉터는
이전 설계서에 기록이 없었는데, 실측 결과 `.txt_price`로 확인.

**다이소몰**: `a.detail-link`(pdNo 기준), `.brand-area .detail-title`(상세
페이지 브랜드) 모두 이전 설계서 기록과 동일하게 유효함. 다만 페이지 전체에
`a.detail-link`가 여러 캐러셀(연관상품 등)에 걸쳐 60개 넘게 섞여 있어, 실시간
랭킹만 정확히 집을 수 있는 `.nav-rank .swiper-slide` 컨테이너로 범위를
좁혀야 한다는 것을 이번에 새로 확인함(이전 설계서에는 이 구분이 명시돼
있지 않았음 — 아마도 그때는 페이지 구조가 더 단순했거나, 다른 캐러셀이
나중에 추가된 것으로 추정).

**올리브영 (중요 변경)**: 이전 프로젝트가 쓰던
`getBestList.do?dispCatNo=900000100100001` 단독 URL로 접속하면, 지금은
"판매랭킹_전체"(코스메틱 포함 전체 베스트)가 반환됨 — 마스크팩, 키보드 등
건강식품과 무관한 상품이 섞여 나왔음. 실제 브라우저로 베스트 페이지의
"건강식품" 필터 버튼을 클릭해 네트워크 요청을 관찰한 결과,
`fltDispCatNo=10000020001`(사이트 카테고리 트리의 건강식품 코드) 파라미터가
추가로 붙는 것을 확인 — 두 파라미터를 함께 써야 원하는 결과가 나옴. 이
변경 없이 이전 코드를 그대로 복사했다면 카테고리 분류기가 전부
"기타/특수목적"이나 엉뚱한 카테고리로 분류하며 조용히 잘못된 데이터를
쌓았을 것 — 전면 재작성 & 실측 검증 과정에서 미리 잡아낸 문제.

**검증 방법**: `python main.py TEST`로 3개 플랫폼 실제 수집 후 결과를
csv로 열어 눈으로 확인. 카카오 20건(2카테고리×10, 광고 정상 제외), 다이소
10건(브랜드 정상 보강), 올리브영 10건(건강식품만 정상 반환) 모두 확인 완료.

---

## 월별 집계 로직 검증 (2026-07-22)

`monthly_aggregate.py`에 이전 프로젝트의 점수 공식(순위70%+꾸준함30%, 기록.md
2026-07-01 인터뷰에서 확정)과 최소등장 완화 로직(2026-07-02 결정), 전월 대비
비교 로직(2026-07-13 결정)을 그대로 이식. 실제 `data/monthly/`를 건드리지
않기 위해 스크래치 디렉토리에 합성 테스트 데이터(연도 2099로 실제 데이터와
겹치지 않게 생성, 상품 15종을 5일치 무작위 샘플링)로 격리 실행:

1. 전월 데이터 없는 첫 달 — 전체 🆕 표시, 순위/가격변동 빈 값 정상 확인
2. 전월 데이터 있는 둘째 달 — 순위변동(▲▼)·가격변동(▲▼원)·카테고리
   증감(%p)이 모두 정확히 계산됨을 확인 (예: 상품5 전월 11위→2.7위 ▲10 정상)
3. 총 상품 종류(15종)가 TOP20보다 적어 최소등장 완화가 1회까지 내려가는
   극단 케이스도 에러 없이 정상 동작 확인

---

## 이번 재구축에서 이식하지 않은 기능

이전 프로젝트에는 있었지만 이번 버전에는 포함하지 않은 것들(설계서.md 9장
참고): 월별요약 시트(핵심지표·1위상품·카테고리추천·급상승급하락·신제품
인사이트), 브랜드통계 시트, Streamlit 대시보드, 카카오톡 실패 알림. 재작성
범위를 "크롤링이 안정적으로 도는 핵심 파이프라인"에 먼저 집중하기 위해
제외함 — 필요해지면 이전 리포 코드를 참고해 추가 가능.

---

## GitHub 리포 생성 (2026-07-22)

리포 이름·공개범위를 확인(Public, `online-mall-ranking`)한 뒤
`gh repo create --source=. --push`로 생성 및 최초 push 완료.
https://github.com/primexx98-sudo/online-mall-ranking — 기본 브랜치는
`master`로 생성됨(이전 프로젝트의 `main`과 다름, 특별히 통일할 필요는 없어
그대로 둠).

## Actions 최초 실행 결과 — 올리브영 재차단 확인 (2026-07-22)

`daily_crawl.yml`을 workflow_dispatch로 수동 실행(실행 ID 29889909880)해
실제 Actions 환경에서 검증:

- 카카오선물하기 20건, 다이소몰 10건 — **정상 수집**
- 올리브영 — `ul.cate_prd_list > li` 셀렉터가 15초 타임아웃까지 나타나지
  않음(`Page.wait_for_selector: Timeout 15000ms exceeded`). 로컬(가정용 IP)
  에서는 같은 코드가 정상 동작했던 것과 대조적 — 이전 프로젝트에서 결론
  내렸던 **Azure 데이터센터 IP 대역에 대한 Cloudflare Turnstile 차단이
  이번 리포에서도 동일하게 재현됨**을 확인.
- `main.py`가 플랫폼 1개 실패 시 `sys.exit(1)`하도록 되어 있어 "Run
  crawler" 스텝은 실패로 표시됐지만, `Commit results` 스텝은 `if: always()`
  라 카카오·다이소 정상분은 그대로 커밋·push됨(`data/daily/2026-07/2026-07-22.xlsx`,
  올리브영 시트만 없는 상태).

**결론**: 인터뷰에서 정한 대로 "자동 수집 재시도부터" 시도했고, 예상했던
차단 패턴이 그대로 재현됐다. 이제 `크롤러_실패시.md`의 캡쳐 기반 수동 입력
절차가 예비책이 아니라 **사실상 상시 운영 절차**가 된 상태 — 매일 카카오·
다이소는 자동, 올리브영은 사용자가 캡쳐를 보내면 그때 반영하는 흐름으로
당분간 운영. IP 레벨 대응(유료 프록시 등)은 여전히 미착수 상태이며, 필요성이
커지면 별도로 재논의.

## Selenium 실증 테스트 결과 — 올리브영 차단 해결 (2026-07-22)

Playwright 기반 올리브영 수집이 Actions에서 재차단된 직후, 사용자가 "Selenium
+ 충분한 sleep"으로 다시 시도해보자고 제안. 코드 레벨 대응을 이미 여러 차례
포기했던 이력이 있어(이전 프로젝트, 이번 프로젝트 모두) 큰 기대 없이
`crawlers/oliveyoung_selenium_test.py` + `test_selenium_oliveyoung.yml`
워크플로를 일회성으로 만들어 `workflow_dispatch`로 4회 반복 실행해 검증.

**결과**: 4회 모두 성공(실행 ID 29893216526, 29893270458, 29893276234,
29893281916). Turnstile 관련 텍스트 없음, `ul.cate_prd_list > li` 100개 정상
렌더링, 첫 상품 "고려은단 비타민C1000 600정" 정상 추출. 우연이 아니라
재현 가능한 결과로 판단.

**추정 원인**: Playwright는 기본적으로 자동화 시그니처(CDP 프로토콜 사용
방식, 특정 JS 속성 등)가 Selenium과 달라 Cloudflare Turnstile의 봇 탐지
휴리스틱에 더 쉽게 걸리는 것으로 보임. `playwright-stealth`로도 못 뚫었던
반면 Selenium은 `--disable-blink-features=AutomationControlled` +
`navigator.webdriver` 오버라이드 + 8~20초 sleep만으로 통과함 — 정확한
탐지 로직은 Cloudflare 비공개라 확정할 수 없음.

**조치**: 테스트 결과가 좋아 `crawlers/oliveyoung.py`를 Selenium 방식으로
전면 교체(브랜드/상품명/가격/URL 필드 추출 로직 포함, classify() 연동은
기존과 동일). `requirements.txt`에 `selenium==4.46.0` 추가(Playwright는
카카오·다이소용으로 유지). 일회성 테스트 파일
(`crawlers/oliveyoung_selenium_test.py`)과 테스트 전용 워크플로
(`test_selenium_oliveyoung.yml`)는 역할이 끝나 삭제. 설계서.md 6장·10장·
11장·12장 반영 완료.

`크롤러_실패시.md`의 캡쳐 기반 수동 입력 절차는 삭제하지 않고 예비책으로
유지 — Selenium도 언젠가 재차단될 가능성은 남아있음.

## daily_crawl.yml 종단 검증 완료 (2026-07-22)

Selenium으로 교체한 코드를 실제 일일 파이프라인(`main.py` 전체 흐름)으로
`workflow_dispatch` 실행(실행 ID 29893684493)해 재검증. 카카오 20건·다이소
10건·**올리브영 10건** 모두 `[OK]`로 정상 수집, `data/daily/2026-07/2026-07-22.xlsx`
정상 저장·커밋·push까지 확인 — 단독 테스트 스크립트가 아니라 운영 흐름
자체에서 통과한 것을 확인했다는 의미가 있음.

## 다음 단계 (미완료)

- [ ] 최소 1개월 데이터 누적 후 `monthly_aggregate.py` 실데이터로 재검증

---

## 두 프로젝트 통합 — 시장조사(Health-Ranking) 폐기 (2026-07-29)

**배경**: 사용자가 `Desktop\업무\시장조사`와 `Desktop\업무\온라인몰 랭킹`
두 폴더를 하나로 합치고 싶다고 요청. GitHub은 이 리포(online-mall-ranking)
기준으로 가고, 시장조사 쪽의 daily/monthly 데이터만 이관 후 나머지(코드,
문서, .git 등)는 전부 삭제하기로 결정. 겹치는 날짜·재구축 당시 보류했던
기능(월별요약·브랜드통계)에 대해서는 사용자에게 옵션을 제시해 방향을 확정:

| 항목 | 결정 |
|------|------|
| 2026-07-22~27 (양쪽에 다 있는 날짜) | 이 리포의 기존 파일 유지(6일 모두 올리브영 정상), 시장조사 쪽 동일 날짜는 버림 |
| 월별취합 재생성 로직 | 시장조사의 발전된 `monthly_aggregate.py`(월별요약+브랜드통계 포함)를 이식해서 재생성 |
| 시장조사 GitHub 리포(Health-Ranking) | 로컬 폴더와 함께 삭제 |

**실행**:
1. 시장조사의 `crawlers/classifier.py`(전체 키워드 세트)·`monthly_aggregate.py`
   (월별요약 9블록+브랜드통계 포함 버전)를 이 리포로 복사해 기존 축약판을
   덮어씀 — 두 파일 모두 이 리포의 `crawlers/config.py` PLATFORMS(딕셔너리)와
   이름은 겹치지만 별도 모듈의 `classifier.PLATFORMS`(리스트)라 충돌 없음을
   확인.
2. 시장조사 daily 엑셀 53개 중 올리브영 시트가 있는 파일만 선별 이관:
   2026-06 전체 30일(전부 올리브영 정상) + 2026-07 중 15일
   (01,02,06,08,09,10,13,14,15,16,17,18,19,20,21). 올리브영 시트 자체가 없는
   날짜(07-03,04,05,07,11,12,23~28)는 미이관 — 사용자가 "엑셀에 올리브영
   없으면 그대로 삭제"라고 명시한 규칙 적용. 07-22~27은 이 리포에 이미
   더 온전한 버전이 있어 미이관(위 표 참고), 07-28은 이 리포가 이미 자체
   수집해 보유 중.
3. 이관된 daily 데이터를 기준으로 이식한 `monthly_aggregate.py`로
   2026-06(30일치)·2026-07(23일치, 위 이관분+이 리포 자체 07-22~29) 월별취합을
   순서대로 재생성(06월을 먼저 만들어야 07월이 전월 대비 비교에 06월을
   전월로 사용). 결과 6시트(월별요약/카카오/다이소/올리브영/카테고리통계/
   브랜드통계) 모두 정상 생성 확인.
4. 설계서.md 4·5·9·12장에 이식 내역·시트 6개 구조 반영.
5. 시장조사 로컬 폴더(`Desktop\업무\시장조사`) 전체 삭제, GitHub의
   Health-Ranking 리포도 `gh repo delete`로 삭제.

**Why**: 두 프로젝트가 사실상 같은 목적(올리브영·다이소몰·카카오 선물하기
판매순위 추적)의 신·구 버전으로 나뉘어 있던 것을 하나로 정리 — 시장조사는
올리브영 Cloudflare 차단으로 이 리포로 완전히 대체된 상태였고
([[project_online_mall_ranking]] 참고), 다만 그동안 쌓인 daily 데이터와
발전된 월별취합 로직(월별요약·브랜드통계)은 버리기 아까워 이관·이식.

**How to apply**: 이후 이 프로젝트 관련 작업 시 `시장조사` 폴더·Health-Ranking
리포는 더 이상 존재하지 않음 — 과거 트러블슈팅 이력이 필요하면 이 기록.md와
위 "이번 재구축에서 이식하지 않은 기능" 절 이전 항목들, 그리고 memory의
[[project_health_ranking]]을 참고(파일 자체는 삭제되었으므로 memory가
유일한 기록).

---

## 카카오·다이소 크롤러를 Playwright → Selenium으로 통일 (2026-07-29)

**배경**: 올리브영만 Selenium, 카카오·다이소는 Playwright로 나뉘어 있던 상태.
카카오·다이소는 차단 이력이 전혀 없어 바꿀 기능적 이유는 없었지만, 사용자가
코드 일관성을 이유로 통일을 요청.

**작업**: `crawlers/base.py`를 Playwright `new_page()`에서 Selenium
`new_driver()`로 교체(올리브영이 쓰던 옵션·webdriver 속성 은폐 스크립트를
공통화). `kakao.py`·`daiso.py`는 브라우저 조작만 Selenium API로 바꾸고, 목록
추출은 기존 Playwright `eval_on_selector_all`에 쓰던 JS 스니펫을 그대로
`driver.execute_script()`로 옮겨 재사용(추출 로직 자체는 변경 없음 — 마이그
레이션 리스크 최소화). 다이소 상세페이지 방문(브랜드·이미지 보강)은
`context.new_page()`(새 탭) 대신 `driver.switch_to.new_window("tab")`으로 동일
효과 구현. `requirements.txt`에서 `playwright`·`playwright-stealth` 제거,
`daily_crawl.yml`의 `playwright install --with-deps chromium` 단계도 삭제.
`python main.py TEST`로 3개 플랫폼 전부(카카오 20건·다이소 10건·올리브영
10건, 이미지·브랜드 포함) 로컬 검증 완료.

**Why**: 순수 일관성 목적의 리팩터라 기능 변경 리스크를 최대한 줄이는 방향으로
접근 — 추출 로직은 그대로 두고 브라우저 조작 계층만 교체.

**How to apply**: 카카오·다이소 셀렉터가 깨지면 이제 `crawlers/kakao.py`·
`crawlers/daiso.py`의 `_EXTRACT_JS`(JS 문자열)를 확인하면 됨 — Playwright
시절과 동일한 위치·문법. 세 크롤러 모두 이제 `new_driver()` 하나만 공유하므로
브라우저 옵션(헤더리스, UA, webdriver 은폐 등) 변경은 `base.py` 한 곳만
고치면 전체에 반영됨.

---

## 월별취합 상품 그룹핑 + 월간점수 공식 개편 (2026-08-05)

**배경**: 사용자가 7월 월별취합 올리브영 시트에서 "2~4위가 전부 등장횟수
2번인 상품인데 이상하다"고 질문. 조사 결과 두 가지 문제가 겹쳐있었음:
① 등장 2회뿐인 상품이 평균순위만 좋으면(예: 2.5위) 15~22회 꾸준히 등장한
상품보다 상위권을 차지하는 점수 공식의 구조적 약점, ② 같은 상품이 날짜마다
프로모션 문구가 미세하게 바뀌어(예: "1일 1정"→"1일1정", 올리브영 goodsNo는
같은데 표시 텍스트만 다름) `상품명+브랜드` 완전일치 그룹핑에서 한 상품이
TOP20 안에 여러 행으로 쪼개지는 버그.

**작업**:
1. **그룹핑**: `build_group_keys()` 신설 — 상품URL의 상품코드(올리브영
   goodsNo/다이소몰 pdNo/카카오 상품ID)를 1순위 키로, URL 파싱 실패 시(수동
   입력 등) 대괄호 프로모션 태그 제거+공백 제거한 정규화 상품명을 2순위
   키로 사용. union-find로 "URL 있는 날"과 "URL 없는 날"의 키가 만나면 병합.
   실데이터 검증: 카카오 오쏘몰(7입/10입/30입/14입 4개 SKU, 문구가 거의
   매일 바뀜)이 URL 기준으로는 정확히 4개로 유지되고, 올리브영 바이탈뷰티
   슬림업(15+2회로 쪼개져 있던 것)이 21회 하나로 통합됨을 확인.
2. **점수 공식**: `평균점수×0.7+등장률×5×0.3` → 베이지안 평균
   `(점수합+K×전체평균)/(등장횟수+K)`(K=5)로 교체. 등장횟수가 적을수록
   전체 평균 쪽으로 점수가 당겨져 표본 부족 왜곡을 완화. 대안으로 가중치
   조정(50/50)·신뢰도 곱연산 페널티(등장 5회 미만 비례 감점)도 시뮬레이션
   했으나, 베이지안 평균이 통계적으로 더 정직하다는 이유로 사용자가 선택.
3. 전월 대비 순위·가격 매칭도 정규화 상품명 기준으로 변경(문구 흔들림 때문에
   "🆕 신규 진입"으로 오판되는 것 방지).
4. 7월 월별취합.xlsx를 새 로직으로 재생성.

**Why**: 그룹핑 버그(②)를 안 고치고 점수 공식(①)만 고치면, 여전히 같은
상품이 여러 행으로 쪼개진 채로 점수만 재계산되는 반쪽 수정이 됨 — 두 문제가
독립적이지만 같은 증상(상위권에 이상한 상품이 보임)으로 나타나서 함께 처리.

**How to apply**: 새 플랫폼을 추가하거나 크롤러의 URL 수집 로직을 바꿀 때는
`_PRODUCT_ID_PATTERNS`에 해당 플랫폼의 상품코드 정규식을 추가해야 그룹핑이
정상 동작함(없으면 자동으로 이름 기반 폴백만 사용 — 동작은 하지만 정확도
낮음). `BAYESIAN_K`(현재 5)를 조정하면 "몇 번 이상 등장해야 온전히 인정할지"
민감도를 바꿀 수 있음 — 낮추면 소수 등장 상품이 더 쉽게 상위권에 들고,
높이면 더 엄격해짐. 같은 날 [[project_health_trend]]의 이커머스 주간·월간
집계(`weekly.py`의 `_aggregate_ecommerce_rankings()`)에도 동일한 베이지안
공식을 이식(K=5로 통일) — 이쪽은 상품명 완전일치 그룹핑 그대로라 ②번
문제는 남아있음, 필요시 이식 검토.
